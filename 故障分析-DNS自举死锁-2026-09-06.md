# 故障分析：mosdns DoH 上游「自举死锁」导致整机周期性卡顿

**日期**：2026-09-06
**设备**：极路由4增强版 HC5962（MT7621，244MB 内存），ImmortalWrt 自编译固件，旁路由 192.168.112.200
**结论一句话**：mosdns 的国内上游里写了两条**域名形式**的 DoH（`doh.pub`、`dns.alidns.com`），而解析这两个域名要绕回本机 DNS 链最上游的 mosdns 自己，形成闭环。平时被 DNS 缓存掩盖，缓存一失效就死锁几十秒，整机所有界面同时卡死，随后自愈。

---

## 一、故障现象

2026-09-06 17:05 前后，用户报告：

| 现象 | 表现 |
|---|---|
| 网页 | 打不开 |
| AdGuard Home 界面（:3000） | 卡住 |
| LuCI 界面（:80） | 进入非常慢 |
| ssr+ 界面 | 完全进不去 |
| SSH（Xshell） | 要重新输密码，且极慢 |
| 持续时间 | 约 30 秒～1 分钟后自行恢复 |

用户原话：「我差点重启路由了」。

同一现象在 **17:36 再次发作**，成为破案关键。

---

## 二、排查过程（含走过的弯路）

### 2.1 先排除的假设

| 假设 | 验证结果 | 结论 |
|---|---|---|
| 内存爆了（OOM） | `dmesg` 无任何 OOM / killed 记录；AGH（PID 3204）、mosdns（4597）进程号未变；可用内存故障时 32MB、之后回升至 37MB | 排除 |
| 服务崩溃重启 | 关键进程启动时间全在 09-05 21:40 或 09-06 02:01（对应 cron 02:00 的订阅更新），**17:05 无任何进程重启** | 是「卡住」不是「崩溃」 |
| Samba / U 盘 IO 拖垮 CPU | 用户确认故障期间未使用 U 盘；`/sys/block/sda/stat` 累计读 1.8GB 但故障时无 D 状态进程 | 排除 |
| lan2 网口频繁掉线 | dmesg 时间戳换算后，lan2 最后一次事件在 **15:42**，故障在 17:05 | 时间对不上，排除 |
| conntrack 表满 | 353 / 31744，无 table full 记录 | 排除 |
| 外网抖动（第一版结论） | 17:36 复发的错误日志直接否定了这一假设 | **已推翻** |

### 2.2 关键数据

```
load average: 0.08, 2.88, 9.32     # 双核 CPU，15 分钟均值 9.32 = 严重过载
logread | grep -c mosdns            # 263 条报错，全部集中在 17:05:10–17:05:39
AGH querylog Elapsed: 9.88~9.89s    # 撞上 AGH 等上游的 10 秒上限
```

报错上游分布（决定性）：

| 上游 | 写法 | 报错次数 |
|---|---|---|
| `https://doh.pub/dns-query` | 域名 | 40 |
| `https://dns.alidns.com/dns-query` | 域名 | 31 |
| `114.114.114.114` | IP | 10 |
| `119.29.29.29` / `223.5.5.5` | IP | 7 |

**写成域名的上游报错 71 次，写成 IP 的只有 17 次。**

---

## 三、根因：自举死锁（bootstrap deadlock）

### 3.1 铁证

17:36:26 复发时抓到的两条日志，把真凶直接招供：

```
daemon.err mosdns[4597]: WARN forward_local upstream error
  {"qname": "doh.pub.", "upstream": "https://doh.pub/dns-query", "error": "context deadline exceeded"}

daemon.err mosdns[4597]: WARN forward_local exchange failed
  {"error": "http request failed: dial tcp: lookup doh.pub on 127.0.0.1:53: server misbehaving"}
```

第一条里 `qname` 与 `upstream` 是同一个域名——**mosdns 正在用 doh.pub 这个上游，去查询 doh.pub 自己**。

### 3.2 死锁环

```
   客户端查询 doh.pub（或任何需要解析 DoH 的场景）
            │
            ▼
   dnsmasq :53
            │
            ▼
   AdGuard Home :5335
            │
            ▼
   mosdns :5353  ── 判断为国内域名 ──▶ forward_local
            │                              │
            │                              ▼
            │                    要连 https://doh.pub/dns-query
            │                              │
            │                              ▼
            │                    先解析 doh.pub 的 IP
            │                              │
            │                              ▼
            └────────◀── 用系统 resolver 127.0.0.1:53 ──┘
                        （回到 dnsmasq，环闭合）
```

mosdns 要连接 DoH 上游，必须先知道这个上游的 IP；而解析这个 IP 用的正是本机 DNS 链——**链条最下游偷偷接回了最上游**，形成闭环。5 秒后 `context deadline exceeded`，解析失败。

### 3.3 为什么是间歇性的

`doh.pub` 的解析结果平时**缓存在 dnsmasq / AGH 里**，mosdns 直接从缓存拿到 IP，环被掩盖，一切正常。

一旦缓存 TTL 到期、又恰好赶上并发查询，环就暴露 → 死锁几十秒 → 超时释放 / 缓存重新填上 → 自愈。这完美解释了「突然卡 30 秒又自己好了」。

### 3.4 为什么旧库固件从不出这事

旧库固件里，AdGuard Home 的上游 DNS 直接填的是**阿里、腾讯的 IP**，压根没有域名解析这一环。写成 IP 的上游不需要解析，天然不成环。

---

## 四、修复

### 4.1 主修复：国内上游只保留 IP

删除 `files/etc/mosdns/config.yaml` 中两条域名型 DoH：

```diff
   - tag: forward_local
     type: forward
     args:
       upstreams:
         - addr: 223.5.5.5
         - addr: 119.29.29.29
         - addr: 114.114.114.114
-        - addr: https://dns.alidns.com/dns-query
-        - addr: https://doh.pub/dns-query
```

**为什么没有副作用**：mosdns 已按 `cn.txt` 完成国内外分流，**防污染是国外链路的职责**（国外走 ssr+ 的 socks5 隧道查 8.8.8.8）。国内域名用 UDP 直问阿里/腾讯/DNSPod 是标准做法，不需要 DoH 那层加密，还省掉每次 TLS 握手——理论上国内解析会更快。

### 4.2 顺带修掉的一处隐患：uci-defaults 执行顺序冲突

| 文件 | 对 `network.lan.dns` 的赋值 | 执行顺序 |
|---|---|---|
| `99-dns-setup` | `127.0.0.1` | 先（字母序 `9` < `z`） |
| `zz-hc5962-custom` | `114.114.114.114` | **后 → 覆盖前者** |

后果：新刷机后路由器本机仍用 114 问 DNS，被污染，ssr+ 网页的「谷歌连通性检查」会永远误报「连接错误」（该检查在路由器本机执行 `/usr/bin/ssr-check`）。

修法：把 `zz-hc5962-custom` 里的值改为 `127.0.0.1`，并在注释里写明原因。

### 4.3 新增：每周三定时重启

写入 `zz-hc5962-custom` 第 9 段（带防重复判断）：

```
0 4 * * 3 sleep 70 && touch /etc/banner && /sbin/reboot
```

本机内存 244MB，常驻 AGH + mosdns + xray + ZeroTier，可用内存长期 30~40MB，每周重启一次清理碎片。注意 AGH 过滤规则存在 `/var`（内存盘），重启后会重新下载，当天上午去广告可能有空窗期。

---

## 五、验证结果（实机，2026-09-06 17:47）

| 项目 | 结果 |
|---|---|
| mosdns 重启 | 成功（PID 15730），监听 127.0.0.1:5353 |
| 国内解析 | `www.baidu.com` → 157.148.69.186，`www.qq.com` → 61.241.54.211 |
| 国外解析 | `www.google.com` → 216.239.38.120（真 IP，未被污染） |
| 环是否断开 | `doh.pub` 现在作为普通域名可正常解析（120.53.53.53）——不再是自己的上游 |
| 新报错 | 无（仅有启动时的 INFO 日志） |

---

## 六、经验教训

1. **链条图纸是直线，运行时可能是环。** 设计图上 `dnsmasq → AGH → mosdns → 上游` 是一条漂亮的单向链，但上游若写成域名，最下游就悄悄接回了最上游。凡是「A 依赖 B 解析，而 B 又要经过 A」的配置都要警惕。

2. **缓存会掩盖设计缺陷。** 这个死锁一直存在，只因为解析结果被缓存而没发作。缓存 TTL 一到期就露馅——这类问题在测试时极难复现，往往上线几周后才出现。

3. **凡是能写 IP 的地方，不要写域名。** 尤其在自引用的系统里（DNS 服务器配上游、代理服务器配远端地址）。

4. **日志洪水会毁掉现场。** mosdns 的 `level: info` 让每条失败查询都写一条 WARN，30 秒写下 229 条，把 OpenWrt 仅 **64KB** 的系统日志环形缓冲（`system.log_size='64'`）冲爆，导致 17:05 那次完全查不到第一推动力——最后是靠 17:36 **再次发作**才抓到证据。
   - 补充坑点：mosdns 走 stderr 输出，syslog 一律标记为 `daemon.err`，所以**在系统层面按级别过滤行不通**，只能在 mosdns 自己的 `level` 上下手。
   - 本次因根因已消除、报错自然消失，暂未降噪；若将来还要保留排查线索，可把 `level` 调到 `error`，或把 `file` 指向 U 盘上的独立日志文件。

---

## 七、相关文件

| 文件 | 说明 |
|---|---|
| `files/etc/mosdns/config.yaml` | 主修复处，注释里已写明「请勿改回」及原因 |
| `files/etc/uci-defaults/zz-hc5962-custom` | 修 DNS 覆盖冲突（第 1 段）+ 定时重启（第 9 段） |
| `files/etc/uci-defaults/99-dns-setup` | 本机 DNS 设 127.0.0.1、AGH/dnsmasq 转发设置 |
| `files/etc/AdGuardHome.yaml` | AGH 配置（端口 5335，上游 127.0.0.1:5353） |

---

## 八、遗留观察项

- **lan2 网口**：dmesg 显示掉线 13 次，现协商速率降至 **10Mbps**（lan3 为 1000Mbps）。已确认与本次故障无关（时间对不上），且 lan2 上流量极小（4569 包 vs lan3 的 840 万包），属独立的物理层问题——怀疑网线或网口接触不良，有机会换根线试试。
- **AGH 免认证**：`/etc/AdGuardHome.yaml` 中 `users: []`，局域网内任何设备可直接改 AGH 设置。家用风险不大，需要时可在 AGH 界面建账号。
- **YouTube 广告**：属 DNS 去广告的天然盲区（广告与视频同域 `googlevideo.com`），需浏览器扩展（uBlock Origin / AdGuard AdBlocker）配合，与固件无关。
