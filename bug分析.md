# 故障分析归档

> 设备：HiWiFi HC5962（MT7621A，mipsel 1004Kc，双核四线程，256MB 内存）
> 固件：ImmortalWrt 自编译（仓库 `fordjkhjk/hiwifi-ImmortalWrt`）
> 角色：旁路由，IP `192.168.112.200`，主路由 / 网关 `192.168.112.1`
> 归档范围：2026-08 底至 2026-09-08
>
> 目的：把踩过的坑、查过的证据、定过的结论集中放一处，避免下次从零重查。
> 每条都尽量给出**可核查的依据**（日志片段、文件路径、实测数字），不写想当然的结论。
> 凡属推论而未证实的，一律标注「未定论」。
>
> 相关文档：
> - 《故障分析-DNS自举死锁-2026-09-06.md》—— 本文 1.1 节的专项详情
> - 《操作手册-手把手.md》—— 编译与刷机步骤
> - `README.md` 第十四节 —— 健康采样机制与崩溃判读

---

## 目录

- [一、已修复](#一已修复)
- [二、已定位、未修复](#二已定位未修复)
- [三、根因未定：9/8 凌晨崩溃专题](#三根因未定98-凌晨崩溃专题)
- [四、误判记录（曾以为是路由器故障，其实不是）](#四误判记录曾以为是路由器故障其实不是)
- [五、硬件与架构固有限制（不是 bug，但必须知道）](#五硬件与架构固有限制不是-bug但必须知道)
- [六、排障工具与踩坑清单](#六排障工具与踩坑清单)

---

## 一、已修复

### 1.1 DNS 自举死锁（最严重，2026-09-06）

| 项目 | 内容 |
|---|---|
| **现象** | 整机周期性卡死：LuCI、SSH、AdGuard Home 界面全断，几分钟后自愈。负载 9.32，日志刷爆 64KB 环形缓冲 |
| **根因** | mosdns 国内上游挂了两条**域名型** DoH：`https://dns.alidns.com/dns-query`、`https://doh.pub/dns-query`。mosdns 要连它们必须先解析其域名 → 走系统 resolver `127.0.0.1:53` → dnsmasq → AGH:5335 → **又回到 mosdns 自己** = 闭环 |
| **为什么平时没事** | 被 DNS 缓存掩盖。**缓存一失效就死锁**，所以表现为"隔一阵卡一次" |
| **证据** | 卡死期间日志刷满该循环；删掉两条后至今未复发 |
| **修复** | 删除两条域名型 DoH，国内上游只留 IP 型 UDP：`223.5.5.5` / `119.29.29.29` / `114.114.114.114` |
| **沉淀的规则** | **上游只写 IP，不写域名**。IP 不需要解析，天然不成环，因而**无需配 bootstrap**。这条规则后来也用于国外上游（选 `https://8.8.8.8/dns-query` 而非 `https://dns.google/dns-query`） |
| **文件** | `files/etc/mosdns/config.yaml`（头部注释已写明"请勿改回"） |
| **专项详情** | 见仓库根目录《故障分析-DNS自举死锁-2026-09-06.md》，本文是汇总索引 |

### 1.2 国外 DNS 隧道 UDP 不通 → 网页版 B 站转圈（2026-09-07）

| 项目 | 内容 |
|---|---|
| **现象** | 国外域名每次解析卡满 **5 秒**超时，最终落到兜底拿污染 IP。网页版 B 站加载海外资源（Google Fonts、统计脚本、广告 SDK）时转圈很久 |
| **根因** | 上游写 `addr: 8.8.8.8`（不带前缀）= **UDP 查询**，走 socks5 的 **UDP ASSOCIATE**，需要机场服务端把 UDP 从隧道还原出去。实测所用机场 **trojan+ws 与 trojan+gRPC 两种传输、共 76 个节点一律不转发 UDP** |
| **关键辨析** | **隧道本身没病** —— 刷 YouTube 走的是同一条隧道，很流畅，因为那是 TCP。死的只是"还原 UDP"这个能力 |
| **证据** | mosdns 日志持续刷 `forward_via_tunnel upstream error upstream=8.8.8.8 error=context deadline exceeded`，elapsed 恒为 5.000s |
| **修复** | 上游改 **IP 型 DoH**：`https://8.8.8.8/dns-query` + `https://1.1.1.1/dns-query`。443 = TCP，走 socks5 的 **CONNECT**，与浏览器同路 |
| **验证** | 20+ 个全新国外域名全部拿到真实 IP，5 秒超时日志归零 |
| **方案 A 为何走到底** | 曾尝试"换节点"，但 76 个节点全属同一家机场，换 ws→gRPC 仍 UDP 不通（22:33:30 实测仍 5 秒超时）。**这是机场服务端策略，不是节点选型问题** |

### 1.3 fallback threshold 500ms 太短（2026-09-07，1.2 的配套坑）

| 项目 | 内容 |
|---|---|
| **现象** | 改成 DoH 后**超时日志没了**，但国外域名仍返回污染 IP |
| **根因** | DoH 首次查询要建 TCP + TLS 走隧道到境外，实测约 **2 秒**；兜底 114 是 UDP 直连，**30ms 就回**。500ms 阈值等于让兜底每次都抢先，把污染结果当答案交上来 —— **主路白干** |
| **修复** | `threshold: 500 → 2500` |
| **验证** | 提阈值后，6 个全新国外域名全部真实 IP |
| **教训** | "没有报错"不等于"修好了"。报错消失但结果仍错，要怀疑是兜底抢跑，得单独测主路 |

### 1.4 AdGuard Home 双实例（2026-09-07）

| 项目 | 内容 |
|---|---|
| **现象** | 两个 AdGuardHome 进程并存 |
| **根因** | `/etc/rc.local` 里有一行手动拉起（早期救急手段），与 init.d 的启动并存 |
| **修复** | 删除 rc.local 那行。**注意：删完仍需重启才生效**（旧进程不会自己退出） |
| **验证** | 重启后单实例，PID 3204 占 3000/5335 |

### 1.5 AdGuard Home 配置文件大小写坑

| 项目 | 内容 |
|---|---|
| **现象** | `uci set` 改 AdGuardHome 配置不生效 |
| **根因** | LuCI 与 init.d 读的是**大写** `/etc/config/AdGuardHome`；用小写 `uci set adguardhome...` 会写进一个孤儿文件 |
| **修复** | 仓库三处修复（commit `9c1c9b5`）；`binpath` 必须改 `/usr/bin/AdGuardHome`（默认值多一层路径） |
| **其余要点** | `configpath=/etc/AdGuardHome.yaml`、`workdir=/var/adguardhome`、`enabled=1`、`redirect='none'` |

### 1.6 nftables.d 顶层 table 声明导致 NAT 全失效

| 项目 | 内容 |
|---|---|
| **现象** | 整个 ruleset 渲染失败，无 NAT |
| **根因** | `/etc/nftables.d/*.nft` 是**被 include 进 fw4 的 table 内部**的，不能写顶层 `table` 声明 |
| **修复** | 去掉文件里的 table 声明，只留规则体 |

### 1.7 旁路由模式下 fw4 不做 masquerade

| 项目 | 内容 |
|---|---|
| **现象** | 旁路由上联只有 br-lan，部分流量不通 |
| **根因** | fw4 默认只对 wan 区做 masq，旁路由场景没有 wan |
| **修复** | `uci set firewall.@zone[0].masq='1'` + commit + reload（已烘焙进 `files/etc/config/firewall`） |

### 1.8 ssr+ 订阅更新失败

早期修复过一次，通过 `diy-part2.sh` 里的 sed 补丁修 `subscribe.lua`。
**具体现象与补丁细节待补**（当时未留存完整记录）。

---

## 二、已定位、未修复

### 2.1 ssr+ 定时任务重复 4 条 / 整条消失（竞态）

| 项目 | 内容 |
|---|---|
| **现象** | crontab 里 ssr+ 的订阅任务有时重复 4 条，有时整条消失 |
| **根因** | 唯一写入点 `/etc/init.d/shadowsocksr` 138–150 行 `add_cron`/`del_cron`，逻辑是 `sed -i '/ssrplus.log/d'` 先删 + `echo >>` 再加。**串行执行是幂等的**（4 次→1 条），**并发执行不幂等**（4 次并发→2 条重复）。整条消失同源：sed 的 rename 覆盖会抹掉别人刚追加的行 |
| **放大因素** | `start()` 的 `set_lock` 用 `flock -xn 1000`（非阻塞）且**从不检查返回值** → 并发 start 全部放行 |
| **佐证** | `/var/lock/ssr-switch.lock` mtime 与节点切换时间吻合；ssr+ 每 5 分钟跑 ssr-rules，节点切换触发重启 → add_cron 高频并发 |
| **状态** | **用户决定不修**（影响可控，改动需打补丁且升级固件会覆盖） |
| **修复方案（备查）** | 给 add_cron/del_cron 加独立 `flock -x 200`；用 `cat tmp > $CRON_FILE` 而非 `mv`（避免丢 600 权限）。落地需在 `diy-part2.sh` 加 sed 补丁，否则升级固件被覆盖 |

### 2.2 ZeroTier 托管路由劫持 LAN 流量 → Samba 慢

| 项目 | 内容 |
|---|---|
| **现象** | Samba 传输只有约 1.3MB/s |
| **根因** | ZT 下发托管路由 `192.168.112.0/24 via <ZT 隧道内地址>`，**metric 291 压过本地 WLAN 的 301**，把 LAN 流量劫持进 ZT 隧道 |
| **状态** | 未修，等用户选方案（调高 metric / 删托管路由 / 在家关 ZT） |

### 2.3 lan2 链路协商在 1G / 10M 之间反复降级

| 项目 | 内容 |
|---|---|
| **现象** | lan2（接一台 HP 电脑，内网 IP `192.168.112.x`）20 小时内断线 14 次，5 次降到 10Mbps；同期 lan3 零翻动 |
| **判据** | **零 CRC 错误**却降速 → 不是线路质量问题，是**协商阶段**降级 |
| **推断** | 网线 8 芯接触不良，或对端网卡节能（EEE） |
| **状态** | 换线后一度恢复 1G，但 09-08 09:15 采样时又发现是 10M。**待用户决定**：换线 / 关对端 EEE / 与 lan3 对调 |

### 2.4 临时 mosdns 实验进程残留

| 项目 | 内容 |
|---|---|
| **现象** | 排查时可用内存从 54MB 掉到 35MB |
| **根因** | 前几轮起了临时 mosdns 实例做对照实验，脚本超时被杀后进程没清理（其中一个 `/tmp/q.yaml` 已残留一天多） |
| **修复** | 手动 kill 后回到 54.7MB |
| **沉淀** | **每次起临时实例，脚本末尾必须 kill + rm；下次查证前先 `ps w \| grep mosdns` 确认无残留** |

---

## 三、根因未定：9/8 凌晨崩溃专题

**这是目前唯一没查清的问题，单独存档。**

### 3.1 完整时间线（采样脚本自动记录，全程未重启，证据完整）

| 时刻 | 可用内存 | Xray 单进程 | 负载 | 内核态 CPU | D 进程 | conntrack |
|---|---|---|---|---|---|---|
| 00:40 | 62.8 MB | 16.6 MB | 0.06 | — | 0 | — |
| 01:00 | 46.6 MB | 27.9 MB | 0.20 | — | 0 | — |
| **01:05** | **10.2 MB** | **69.8 MB** | **15.87** | 73–79% | 4 | 493 |
| 01:10 | 9.5 MB | 69.9 MB | — | — | 2 | 324 |
| 01:20 | 11.3 MB | 69.9 MB | — | — | 9 | **186** |
| 01:36 | 15.0 MB | 69.9 MB | 26.56 | 73–79% | 5 | 287 |
| 01:40 | 56.1 MB | 30.3 MB | 1.95 | — | — | — |

**5 分钟内 Xray 从 27MB 涨到 69.8MB**，可用内存被抽干，随后：
内核疯狂回收 → UBI 闪存 I/O 阻塞 → `[kworker/N:x+ubi]` 卡 D 状态 → 全机假死 31 分钟后自愈。

### 3.2 已推翻的假说：DoH 并发导致连接堆积

一度写进结论的说法：

> mosdns 每个 DoH 查询经 socks5 由 Xray 建 TLS 连接，Go 默认只复用 2 条，
> 并发一多就大量新建，每条 TLS 上下文加缓冲区，100 多条就是 40MB —— 跟暴涨量正好对上。

**三处硬伤，已撤回：**

1. **逻辑反了**：握手超时的连接是**失败的**，失败即释放，不占 TLS 上下文。把失败迹象当占用证据，方向反了。
2. **算术不成立**：40MB ÷ 100 条 = 每条 400KB；而 Go 的 TLS 连接加读写缓冲实测量级是 32～64KB/条，差 6～12 倍。凑够 40MB 要 600～1200 条，而当前整机出站 443 连接只有 3 条。
3. **前提站不住**：凌晨 1:05 家里设备基本闲置，且 mosdns 上游还有 AGH 缓存兜底，不可能有上百个并发国外查询。

**决定性反证（时间序列）**：

> 01:05 → 01:20，conntrack 从 493 降到 **186（少 62%）**，
> Xray 却从 69.8MB 变成 **69.9MB —— 一个 KB 都没降**。
> 如果内存真是连接对象占的，这不可能发生。

**另一个反证**：07:24 那批 TLS 握手超时刷屏时，Xray 稳定 25MB 纹丝不动。

### 3.3 修正后的模型：共因的两个平行症状

```
                链路拥塞 / 节点质量差（原因未确定）
                    │
        ┌───────────┴───────────┐
        ▼                       ▼
  症状 A：握手超时          症状 B：缓冲堆积
  连接建不完，日志刷屏      在途数据堆在 Xray，RSS 暴涨
```

A 和 B 是**兄弟，不是父子**。07:24 那次只有 A 没有 B，正说明两者可分离。

### 3.4 当前判断

- **能确定的**：40MB 的去向是 Xray（有 RSS 实测），且 31 分钟后能回落 → **不是泄漏**，是 Go 堆的高水位特性（涨上去不立刻归还 OS）。
- **最可能但未经证实**：Xray 的 in-flight 数据缓冲（buf pool），而非连接上下文。40MB 这个量级对应数据缓冲更合理。
- **盲区**：采样脚本当时**没记流量**，所以无法判断 01:05 有多少吞吐。

### 3.5 已做的兜底与补强（2026-09-08）

| 措施 | 内容 |
|---|---|
| **阈值重启** | Xray **单进程** RSS > 50MB 重启 shadowsocksr，30 分钟冷却。不依赖根因，直接把崩溃链条掐断 |
| **采样补字段** | 新增 `xray.max/tot/fd`、`dohconn`（到 8.8.8.8/1.1.1.1 的 443 连接数）、`net`（br-lan 收发速率）→ 下次崩溃可直接区分"流量驱动"还是"连接驱动" |

阈值口径说明见 README 第十四节。

---

## 四、误判记录（曾以为是路由器故障，其实不是）

### 4.1 9/7 22:52 "崩溃" —— 其实没崩，是我误判并白重启了一次

| 项目 | 内容 |
|---|---|
| **当时的判断** | LuCI 显示「CPU 94%、内存 16MB」→ 判定崩溃 → **强制重启** |
| **事后查证** | 闪存采样显示全程正常：22:50 avail 64.8MB / load 0.13；22:55 62.6MB / 0.20；23:00 64.6MB / 0.89。**内核态 CPU 1%，D 进程 0** |
| **真相** | 更可能是 LuCI / uhttpd 自己卡了，或读到了瞬时抖动。**强制重启白做，还销毁了现场** |
| **沉淀的规则** | ① **重启前必须先问用户**（家里有人打游戏）；② **故障时优先取证**，别急着恢复 —— 恢复动作会销毁现场；③ LuCI 的瞬时读数不能直接当结论，要看采样趋势 |

### 4.2 vivo 手机 SMB 看电影"先快后降速"

| 项目 | 内容 |
|---|---|
| **现象** | 手机播 Samba 电影，开头 6MB/s，1–3 秒后降速或断流。iPad 和电脑正常，老 LEDE 固件正常 |
| **查证** | 513 个采样点**重传全为 0**，突发峰值 5.8MB/s。形态是"冲 2 秒、停 37 秒、换条新连接再来" |
| **真相** | 不是路由器问题。① CX 文件管理器（Java SMB 实现）是"读一块→交给播放器→等吃完→再读"的节奏；② 服务端从老 LEDE 的 **samba3** 换成了 **ksmbd**，samba3 有 oplock + 顺序预读会主动多喂，ksmbd 是"你问一次我给一次"，不照顾弱客户端 |
| **为什么 VidHub 正常** | 原生 SMB3 客户端，大预读、长连接、持续流式读 |
| **已做** | ksmbd 的 `smb2 max read/write/trans` 由 64K 改 1M 并开缓存（保留，无害，但**不是关键修复**） |
| **建议** | 用 VidHub / VLC / nPlayer 直接开 `smb://`，绕开 CX 这座桥 |

### 4.3 电脑访问 Samba 提示无权限

属于 Windows 默认拦截不安全来宾登录，非路由器问题。
需管理员执行：`reg add "HKLM\SYSTEM\CurrentControlSet\Services\LanmanWorkstation\Parameters" /v AllowInsecureGuestAuth /t REG_DWORD /d 1 /f`，然后 `net use * /delete /y`。

---

## 五、硬件与架构固有限制（不是 bug，但必须知道）

### 5.1 内存：244MB 可用，安全垫仅 40～60MB

| 占用方 | 常态 |
|---|---|
| AdGuard Home | 30–40 MB |
| Xray ×2 | 17–20 MB/进程 |
| mosdns | 15–25 MB |
| dnsmasq | 15 MB |
| ZeroTier + 内核 SUnreclaim | 29 MB |
| **可用余量** | **40–60 MB** |

任何进程涨 30–40MB 就会触底。**这是结构性约束，调参治不了根。**

### 5.2 无 swap、无 zram —— 一触底就是硬着陆

| 检查项 | 结果 |
|---|---|
| `/proc/swaps` | 空 |
| `swappiness` | 60（**内核支持 swap**，只是没有 swap 设备） |
| `/lib/modules/5.15.198/zram.ko` | **不存在** |
| zsmalloc / lzo / lz4 模块 | **全都没有** |

内核编译时未开 `CONFIG_ZRAM`。要上 zram 得**重编内核**，不是装个包的事。
且 MIPS 上压缩解压吃 CPU，崩溃时内核态 CPU 已 73–79%，可能反而更卡。**暂缓。**

> 常见误解：`SwapTotal=0` 只是**没启用 swap 设备**，不等于内核不支持。
> `openwrt/config/Config-kernel.in` 里 `KERNEL_SWAP ... default y if !SMALL_FLASH`。

### 5.3 无硬件加密 → v2ray 是 CPU 天花板

MT7621 没有可用硬件加密模块（无 EIP-93）。AES 加解密 100% 软算，
按 15 秒实测（双向约 2.8Mbps 占掉约 1 个核）推算，**v2ray 天花板约 15–25Mbps**。
1080p YouTube 安全，4K 会卡在 CPU 上（表现为转圈但网速没满）。

> 别想着加硬件加密模块降 CPU —— **对 v2ray 无效**，v2ray 是 Go 写的，
> 用 Go 自带 crypto，不走内核加密接口。

### 5.4 日志在内存盘，重启即清

syslog / dmesg / AGH 查询日志全在 tmpfs，且本机**无 `/sys/fs/pstore`**。
`logread` 是 64KB 环形缓冲，崩溃时**起点常被刷掉**。

**这就是为什么要建健康采样双写机制**（`/root/health-flash.log` 闪存留存）。
9/6 那次就是吃了这个亏，9/8 那次靠它抓到了完整现场。

### 5.5 旁路由对不同设备的意义不同

| 设备网关 | 流量路径 | 旁路由崩了会怎样 |
|---|---|---|
| 指向主路由 `.1` | 走 mt7530 **硬件二层转发**，不过 CPU、不进 conntrack | **照常上网** |
| 指向 `200` | 完全依赖 CPU 侧软件栈 | **一崩全断** |

可用于判断故障范围。

### 5.6 内核模块缺失：kmod-nft-socket

ssr+ 日志持续刷「nftables 透明代理缺失基础依赖 kmod-nft-socket」。
固件未编此模块，与上述故障无因果关系，仅记录。

---

## 六、排障工具与踩坑清单

### 6.1 这台 busybox 的坑

| 坑 | 说明 |
|---|---|
| 无 `bash` / `nohup` / `ethtool` / `dig` / `timeout` / `ss` | 只有 busybox 的 ash 和精简工具 |
| **`sed` 不认识含 `https://` 的文本** | 改用 `grep -v` 过滤 |
| **嵌套引号会被吃** | 复杂命令**必须写成 .sh 文件再传**，不要直接在命令行拼 |
| **`awk '/^Rss:/...'` 取不到值** | 必须用 `grep VmRSS /proc/PID/status` |
| `cat -n` 不支持 | 用 `cat` |
| `find -size` 只认 b/c/k | 用 `+200000k` 这种写法 |
| `ping -i 0.3` 不支持 | 默认 1 秒 |
| **eth0 计数已溢出** | tx 顶到 `2147483647`（2³¹−1，32 位上限）。**算速率必须用 br-lan** |
| 传文件前必须 `tr -d '\r'` | 本机 autocrlf=true |
| **heredoc 过长会导致 SSH 通道断** | 改用 SFTP 上传（见 `upload.py`） |

### 6.2 网络排查的坑

| 坑 | 说明 |
|---|---|
| **路由器本机发出的 UDP 53 全被 ssr+ 劫持** | 试 `nslookup X 114.114.114.114`、`1.1.1.1`、`223.5.5.5` **全部**返回 `2001::1`（黑洞地址）。在这台机器上做"指定外部 DNS 的对照测试"是**无效**的，只能走 127.0.0.1 |
| **conntrack 不跟踪回环** | 实测含 127.0.0.1 的仅 7 条。**别拿 conntrack 数当 DoH 连接数用** |
| `nslookup` 不支持指定端口 | 用 `127.0.0.2:53` 起临时实例绕开（该地址不被劫持） |
| LuCI 改 JS 看不到效果 | 浏览器缓存（uhttpd 不发 Cache-Control）。破缓存靠改视图文件名 + menu.d 的 action.path |
| `agent-browser` 在本机沙箱起不来 | 别重试 |

### 6.3 操作规范

| 规则 | 原因 |
|---|---|
| **重启路由器前必须先问用户** | 家里有人在用网（lan2 那台在打游戏） |
| **故障时优先取证，别急着恢复** | 恢复动作（重启/重装/回滚）会销毁现场 |
| 崩溃后先试 SSH（卡但常能连上） | 抓 top/free/conntrack/dmesg/各进程 RSS |
| ADH 缓存 TTL 按上游 TTL 走 | `cache_ttl_min/max=0`。改完 DNS 后旧污染 IP 会残留到 TTL 过期，属正常 |
