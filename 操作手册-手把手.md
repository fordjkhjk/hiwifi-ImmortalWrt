# 手把手操作手册：从零开始用 GitHub 编译固件

这份文档假设你**从没用过 GitHub**。每一步都写清楚点哪里、填什么、看到什么算成功。

已经确认过你本机的情况：

- ✅ **git 已安装**（version 2.55.0），不需要再装任何东西
- ✅ 配置包在 `C:\Users\Ford Yang\WorkBuddy\2026-08-30-21-02-23\outputs\immortalwrt-hc5962-build`

先说清楚整体在干什么，免得每步都云里雾里：

> GitHub 提供一个**免费的云端 Linux 电脑**。我们把编译配置传上去，
> 然后让那台云端电脑按配置下载 ImmortalWrt 源码、编译、打包，
> 最后把编译好的固件文件放在网页上给你下载。
>
> 整个过程你的电脑什么都不用干，也不用开着。

---

## 第 0 步：确认有 GitHub 账号

打开 <https://github.com>

- 右上角如果显示 **Sign up**，说明没登录过 → 点它注册（邮箱 + 密码 + 用户名，全程中文界面可选）
- 如果已经能看到你的头像，说明已登录，**记住这个用户名**，后面要用

> 注册时如果收不到验证码邮件，检查垃圾邮件；Gmail / Outlook 比国内邮箱稳。

---

## 第 1 步：新建一个仓库

「仓库」就是一个存放文件的网上文件夹。

1. 登录 GitHub 后，看页面**最右上角**，找到你的**头像**，点它
2. 在下拉菜单里点 **Your repositories**（你的仓库）
3. 页面右侧有个绿色的 **New** 按钮，点它

> 另一个入口：页面最右上角有个 **`+`** 号（在头像左边），点它 → **New repository**

填写创建页面：

| 项目 | 填什么 |
| --- | --- |
| Owner（所有者） | 默认就是你的用户名，不用动 |
| Repository name（仓库名） | 填 `hiwifi-ImmortalWrt`（只能英文数字和横杠） |
| Description（描述） | 选填，空着也行 |
| Public / Private | **必须选 Public**（公开） |

**为什么必须 Public**：路由器固件里的在线升级脚本要从 GitHub 匿名下载固件，
私有仓库的下载链接需要登录令牌（token），而令牌最长一年就过期——到期后升级功能
会悄悄失灵。公开仓库的下载链接永远有效。固件配置里有一个内网地址
`192.168.112.200` 会跟着公开，但它是内网私有地址，外网无法访问到你的路由器，
不用担心。

**⚠️ 下面这三个一定不要勾：**

- `Add a README file`
- `Add .gitignore`
- `Choose a license`

这三个会让仓库"不空"，给后面的上传添麻烦。全都不动，保持空白。

点最下面的绿色 **Create repository** 按钮。

**✅ 成功标志**：进入一个新页面，中间有一段标题为
「Quick setup — if you've done this kind of thing before」的提示，
上面显示着你的仓库地址，长得像：

```
https://github.com/你的用户名/hiwifi-ImmortalWrt.git
```

**把这个地址记下来**（就是你的仓库地址，下面要用）。

---

## 第 2 步：把配置文件传上去

### 2.1 打开终端

按键盘上的 **Win 键**，直接输入 `powershell`，回车。
会弹出一个蓝色（或黑色）的窗口，这就是终端。

> 也可以用：打开「此电脑」进入 `C:\Users\Ford Yang\WorkBuddy\2026-08-30-21-02-23\outputs\immortalwrt-hc5962-build`，
> 在文件夹空白处**右键** → 「在终端中打开」（Windows 11 有这个选项）。

### 2.2 设置你的身份（一辈子只做这一次）

把下面两行里引号内的内容**换成你自己的**，然后复制粘贴到终端，回车：

```powershell
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的GitHub注册邮箱"
```

比如你的 GitHub 用户名叫 `fordyang`，邮箱是 `ford@163.com`，那就是：

```powershell
git config --global user.name "fordyang"
git config --global user.email "ford@163.com"
```

> 这两条命令没有任何输出就是成功了。它只是告诉 git 你是谁，方便记录"谁改的"。

### 2.3 进入配置包目录

复制粘贴这一行，回车：

```powershell
cd "C:\Users\Ford Yang\WorkBuddy\2026-08-30-21-02-23\outputs\immortalwrt-hc5962-build"
```

> 路径两侧必须有英文双引号，因为里面有空格。

### 2.4 上传到 GitHub

**逐条**复制粘贴执行（一次一条，等它跑完再下一条）：

```powershell
git init
```

```powershell
git add -A
```

```powershell
git commit -m "ImmortalWrt 23.05 HC5962 build config"
```

```powershell
git branch -M main
```

```powershell
git remote add origin https://github.com/你的用户名/hiwifi-ImmortalWrt.git
```

> ⚠️ 最后这条要把 `你的用户名` 换成**你自己的 GitHub 用户名**。
> 地址就是第 1 步结束时让你记下的那个。

最后上传：

```powershell
git push -u origin main
```

### 2.5 这一步会要求登录（新手最容易卡在这）

执行 `git push` 后，**通常会弹出一个窗口让你登录 GitHub**。有两种情况：

**情况 A：弹出了 GitHub 登录窗口（或自动打开浏览器）**

直接用浏览器登录并点绿色的 **Authorize** 授权按钮即可。
成功后终端会显示一堆 `Writing objects` 之类的字，最后停在命令行提示符。

**情况 B：终端里要你输用户名密码，或者报错说认证失败**

GitHub 从 2021 年起**不接受密码登录**了，要用一个叫「访问令牌」的东西代替密码。按这个做：

1. 打开 GitHub 网页，点右上角**头像** → **Settings**（设置）
2. 页面左侧的菜单**拉到最底下**，点 **Developer settings**（开发者设置）
3. 点 **Personal access tokens** → **Tokens (classic)**
4. 点右上角 **Generate new token** → 再点 **Generate new token (classic)**
5. 按下面填：
   - **Note**：随便写，比如 `firmware-build`
   - **Expiration**：选 `90 days`
   - **Select scopes**：**只勾选第一项 `repo`**（勾它会自动把它下面的子项全选上）
6. 拉到页面最底部，点绿色 **Generate token**
7. **页面上会显示一串字符（以 `ghp_` 开头）——立刻复制保存好**
   ⚠️ 这个串**只显示这一次**，关掉页面就再也看不到了，只能重新生成

8. 回到终端，重新执行 `git push -u origin main`，登录时：
   - **用户名**：填你的 GitHub 用户名
   - **密码**：**粘贴刚才那串 `ghp_` 开头的令牌**（不是你的 GitHub 密码）
   
   > 粘贴密码时终端不会显示任何字符（连 `*` 都没有），这是正常的防窥设计，
   > 直接粘贴完按回车就行。

**✅ 成功标志**：浏览器刷新你的仓库页面，能看到这些文件：

```
.github/
configs/
files/
diy-part1.sh
diy-part2.sh
feeds.conf.default
README.md
操作手册-手把手.md
```

看到这些，说明文件已上传成功。

---

## 第 3 步：开启 Actions 写权限（只做一次）

这步不做的话，编译能跑完，但固件传到 Release 会失败（Artifacts 不受影响，固件照样能下）。
为了不留隐患，还是设一下。

1. 在你的仓库页面，点顶部的 **Settings**（设置，在最右边那排标签里）
2. 左侧菜单点 **Actions** → **General**
3. 页面往下滚，找到 **Workflow permissions**（工作流权限）这一块
4. 选中 **Read and write permissions**（读写权限）
5. 点旁边的 **Save** 按钮

> 如果弹出密码确认，输入你的 GitHub 密码确认。

---

## 第 4 步：启动编译（手动触发，第一次必做）

> **这一步只在第一次刷机前做一次。** 目的是手动触发编译，同时拿到
> minimal 的 `factory.bin`（Breed 首刷用）和 full 的 `sysupgrade.bin`。
> 刷机完成之后，从 10 月 1 日起 GitHub 每季度自动编译新版本，
> 就再也不用来这里手动按了。

1. 点仓库页面顶部的 **Actions** 标签
   （在 `Code` 和 `Projects` 之间，图标是个播放键）
2. 如果是第一次用，页面中间可能有黄色提示
   「Workflows aren't being run on this forked repository」，
   点绿色的 **I understand my workflows, go ahead and enable them**
3. **左侧**列表里点 **ImmortalWrt HC5962 Builder**
4. **右侧**会出现一个灰色的 **Run workflow** 下拉按钮，点它
5. 展开后有个下拉框 `编译档位`，**选 `both`**（一次同时编出精简版和完整版）
6. 点绿色的 **Run workflow** 按钮

**✅ 成功标志**：页面刷新后，列表里多出两条正在运行的任务，
前面有个**黄色的小圆点在转**，名字类似 `build (minimal)` 和 `build (full)`。

---

## 第 5 步：等待（20 ~ 40 分钟）

编译在 GitHub 的服务器上跑，你的电脑可以关掉、可以睡觉、可以关机，**不影响**。

想看进度就点进那条任务，会看到一堆步骤在逐条执行：

| 步骤名 | 在干什么 |
| --- | --- |
| Initialization environment | 装编译需要的软件 |
| Clone source code | 下载 ImmortalWrt 源码 |
| Load custom feeds / Update feeds | 下载插件源 |
| Download package | 下载各种依赖包（**这步最慢**） |
| Compile the firmware | 正式编译（**第二慢**） |
| List firmware size | 报告固件体积 |
| Upload firmware directory | 打包上传 |

**看日志**：点某个步骤名，能看到它实时输出的文字在滚。

**三种结局：**

- 🟢 **绿色对勾** = 成功，去看第 6 步
- 🔴 **红色叉** = 失败，翻到本文档最后的「出问题怎么办」
- 🟡 **黄色圆点** = 还在跑，继续等

---

## 第 6 步：下载固件

编译成功后，点进那条**绿色对勾**的任务记录，页面**拉到最底下**，
有个 **Artifacts**（构件）区域，里面有两个压缩包：

```
OpenWrt_firmware_minimal_hiwifi_hc5962_20260831XXXX
OpenWrt_firmware_full_hiwifi_hc5962_20260831XXXX
```

分别点名字下载（会下载成 zip 文件）。解压后：

```
minimal 包
  └── *factory.bin       ← 第 1 步刷机用这个
      *sysupgrade.bin

full 包
  └── *sysupgrade.bin    ← 第 2 步升级用这个
```

完整文件名大概长这样：

```
immortalwrt-ramips-mt7621-hiwifi_hc5962-squashfs-factory.bin
immortalwrt-ramips-mt7621-hiwifi_hc5962-squashfs-sysupgrade.bin
```

**认准 `factory` 和 `sysupgrade` 这两个关键词就够了。**

> 💡 另一个下载入口：如果第 3 步的权限开了，固件还会出现在仓库首页右侧的
> **Releases** 区，名字带 `minimal-` 或 `full-` 前缀，那里下载不用登录。

---

## 出问题怎么办

### `git` 不是内部或外部命令

说明 git 没装好，或没加到环境变量。
去 <https://git-scm.com/download/win> 下载安装，一路点 Next 用默认选项即可，装完重开终端。

### `cd` 提示找不到路径

路径里有中文和空格，**必须带英文双引号**：

```powershell
cd "C:\Users\Ford Yang\WorkBuddy\2026-08-30-21-02-23\outputs\immortalwrt-hc5962-build"
```

### push 时报 `error: failed to push some refs`

多半是第 1 步那三个勾选项没空着（仓库里已有 README 或 .gitignore）。解决：

```powershell
git pull origin main --rebase
git push -u origin main
```

### 编译红色叉，日志里出现 `too big`

说明 minimal 的 factory.bin 超过 32MB 了。
翻到 README 的「九、体积控制：只有 factory 需要操心」，按里面的顺序注释掉几项，
然后重新上传、重新编译。

### 编译红色叉，日志里出现 `check-size`

同上，只有 factory.bin 会触发这个。检查是不是 full 档位误生成了 factory
（`diy-part2.sh` 里已经处理了这个情况，正常不会遇到）。

### 编译卡在 Download package 超过 1 小时

GitHub 服务器偶尔抽风，直接取消任务重新跑一次即可。

### 想重新编译（改了配置之后）

改完 `configs/` 里的文件后，回到配置包目录重新执行：

```powershell
git add -A
git commit -m "调整配置"
git push
```

然后回 GitHub 的 Actions 页面重新点 Run workflow。

---

## 编译完成后：刷机

拿到两个 bin 文件后，照 README 的「五、刷机流程（Breed）」操作：

1. **Breed 刷入 minimal 的 `factory.bin`** → 路由器 IP 变成 192.168.112.200
2. 浏览器打开 <http://192.168.112.200>（用户名 `root`，密码 `password`）
3. **系统 → 备份/升级** → 上传 **full 的 `sysupgrade.bin`** → 不勾选保留配置
4. 重启完成，插件全部就位

刷机过程中任何一步卡住，随时来问。

---

## 以后的日常升级（每季度一次）

> **注意：只有第一次**需要你手动触发编译（就是上面第 4 步，选 `both`，
> 为了拿到 factory.bin 首刷）。刷机完成后，从 10 月 1 日起 GitHub
> 每季度自动编译新版本，下面的步骤只管以后的事。

这套配置已经带全自动编译 + **网页一键升级**。每个季度（1/4/7/10 月初）
GitHub 会自动编译新固件，你要做的只是：

**第 1 步：看看有没有新版本（网页上看就行）**

1. 浏览器打开 <http://192.168.112.200>，登录 LuCI
2. 进 **系统 → 固件升级**
3. 点 **检查更新** 按钮（自定义命令里的「检查固件更新」按钮也行，同一个脚本）
4. 页面会显示当前版本和最新版本：
   - 没有新版本 → 不用动，结束
   - 有新版本可升级 → 继续第 2 步

**第 2 步：网页一键升级（日常推荐，全程盯着，约 5 分钟）**

1. 就在 **系统 → 固件升级** 页面，看到新版本后
2. 在「确认词」输入框里输入 `upgrade`（英文小写，防误点，输入后按钮才生效）
3. 点 **开始升级**
4. 页面实时显示升级日志：下载固件 → sha256 校验 → 试刷校验 → 刷入
5. 看到「刷入完成」提示后，**千万别拔电源**，等 2-3 分钟自动重启
6. 重启完，浏览器打开 <http://192.168.112.200> 确认一切正常

> 升级过程中按钮会消失、页面一直轮询显示日志，不会重复触发。
> 万一网页卡住或页面出问题，SSH 兜底还在（下方「备用方案」）。

**备用方案：SSH 命令行升级（和网页版流程完全一致）**

1. 电脑上按 **Win 键**，输入 `cmd`，回车，打开命令行窗口
2. 输入（第 1 次连接会问 `Are you sure you want to continue connecting`，
   输 `yes` 回车；然后输密码 `password`，输的时候屏幕不显示，正常）：

   ```
   ssh root@192.168.112.200
   ```

3. 登录进去后，输入：

   ```
   fw-upgrade
   ```

4. 脚本会自动：查版本 → 检查内存空间 → 下载固件（约 29MB，1-3 分钟）→
   校验完整性 → 验证镜像 → 最后**问你一句 `确认刷入? [y/N]`**
5. 输 `y` 回车，之后什么都别动，**千万别拔电源**，等 2-3 分钟自动重启
6. 重启完，浏览器打开 <http://192.168.112.200> 确认一切正常

**注意事项**：

- 升级**保留**你所有设置（无线密码、ssr+ 节点、AdGuard Home 配置都在）
- 但**用 iStore 手动装过的插件会被清掉**，要重新装一遍——这就是当初把
  常用插件烘焙进固件的原因
- 万一升级失败刷坏了：断电 → 按住 Reset → 通电进 Breed 重刷，退路永远在

