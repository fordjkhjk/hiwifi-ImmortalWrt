#
# 档位 A / minimal —— 精简版，用于 Breed 首次刷入
#
# 目标: HiWiFi HC5962 极路由4增强版 (MT7621, 128MB NAND, 256MB RAM)
# 硬限制: IMAGE_SIZE = 32768k (32MB)，超过会 check-size 失败
#
# 本档位只放「小体积 + 必须内置」的东西：
#   中文 LuCI / USB 全套驱动 / U 盘自动挂载 / ksmbd 文件共享 / iStore 商店
# 不含: ssr+、AdGuard Home（体积大，放在 full 档位）
#
# 预估体积: 10 ~ 13 MB
#

# ===== 目标机型 =====
CONFIG_TARGET_ramips=y
CONFIG_TARGET_ramips_mt7621=y
CONFIG_TARGET_ramips_mt7621_DEVICE_hiwifi_hc5962=y

# ===== LuCI 与中文 =====
CONFIG_PACKAGE_luci=y
CONFIG_LUCI_LANG_zh_Hans=y
CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
# 主题（bootstrap 已随 luci 附带，argon 是 ImmortalWrt 常用主题，可选）
CONFIG_PACKAGE_luci-theme-argon=y
CONFIG_PACKAGE_luci-app-argon-config=y
CONFIG_PACKAGE_luci-i18n-argon-config-zh-cn=y

# ===== USB 支持（U 盘自动挂载的基础）=====
# kmod-usb-core / kmod-usb3 由机型与依赖自动带入
CONFIG_PACKAGE_kmod-usb2=y
CONFIG_PACKAGE_kmod-usb3=y
CONFIG_PACKAGE_kmod-usb-storage=y
CONFIG_PACKAGE_kmod-usb-storage-uas=y
CONFIG_PACKAGE_kmod-usb-storage-extras=y
CONFIG_PACKAGE_kmod-usb-ledtrig-usbport=y

# ===== 文件系统支持 =====
CONFIG_PACKAGE_kmod-fs-ext4=y
CONFIG_PACKAGE_kmod-fs-exfat=y
CONFIG_PACKAGE_kmod-fs-vfat=y
CONFIG_PACKAGE_kmod-fs-msdos=y
CONFIG_PACKAGE_kmod-fs-ntfs3=y
# NTFS 写入支持（ntfs-3g 约 1MB，需要时可开）
# CONFIG_PACKAGE_ntfs-3g=y
CONFIG_PACKAGE_kmod-nls-utf8=y
CONFIG_PACKAGE_kmod-nls-base=y
CONFIG_PACKAGE_kmod-nls-cp437=y
CONFIG_PACKAGE_kmod-nls-iso8859-1=y

# ===== 自动挂载 =====
# automount 是 ImmortalWrt 官方 package/emortal/automount 提供的热插拔脚本，
# 插入 U 盘后自动挂载到 /mnt/sda1 一类路径，并自动写入 fstab
CONFIG_PACKAGE_automount=y
CONFIG_PACKAGE_block-mount=y
CONFIG_PACKAGE_e2fsprogs=y
CONFIG_PACKAGE_lsblk=y
CONFIG_PACKAGE_blkid=y
CONFIG_PACKAGE_fdisk=y
CONFIG_PACKAGE_badblocks=y

# ===== SMB 文件共享: ksmbd（内核态，约 300KB，比 samba4 省 ~10MB）=====
CONFIG_PACKAGE_luci-app-ksmbd=y
CONFIG_PACKAGE_luci-i18n-ksmbd-zh-cn=y
CONFIG_PACKAGE_ksmbd-server=y
CONFIG_PACKAGE_kmod-fs-ksmbd=y
# Windows 网络发现（让"网上邻居"/"网络"里能直接看到路由器，约 100KB）
CONFIG_PACKAGE_wsdd2=y
# 关闭 samba4（体积巨大，minimal 档位不要）
# CONFIG_PACKAGE_luci-app-samba4 is not set
# CONFIG_PACKAGE_samba4-server is not set

# ===== iStore 软件商店 =====
# 注意: iStore 本体就是中文原生界面，没有 po 目录，因此不存在 luci-i18n-store-zh-cn 这个包
CONFIG_PACKAGE_luci-app-store=y
CONFIG_PACKAGE_taskd=y
CONFIG_PACKAGE_luci-lib-taskd=y
CONFIG_PACKAGE_luci-lib-xterm=y

# ===== 防火墙: 以默认的 firewall4 (nftables) 为准 =====
# ImmortalWrt 23.05 默认装的是 firewall4 + nftables（include/target.mk 的
# DEFAULT_PACKAGES.router）。早期"显式切 firewall3"的尝试实测被 defconfig
# 静默推翻（firewall=y 降级成 =m，fw4 照装），详见 config-full.config 的
# 防火墙段落。自定义规则统一走 /etc/nftables.d/*.nft，与 full 档位一致。
# LuCI 防火墙管理页（fw4 的前端，要保留）
CONFIG_PACKAGE_luci-app-firewall=y
CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y
# iptables-zz-legacy 保留：minimal 虽不含 ssr+，但为与 full 保持一致，
# 且 ipset/iptables 工具链在 minimal 里也有用途。
# iptables 是虚拟包名（iptables-zz-legacy 与 iptables-nft 都 PROVIDES:=iptables），
# 显式指定 legacy 版避免歧义
CONFIG_PACKAGE_iptables-zz-legacy=y
CONFIG_PACKAGE_ip6tables-zz-legacy=y
CONFIG_PACKAGE_iptables-mod-nat-extra=y
CONFIG_PACKAGE_kmod-ipt-nat6=y
CONFIG_PACKAGE_kmod-ipt-offload=y

# ===== 维护工具 =====
CONFIG_PACKAGE_openssh-sftp-server=y
CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_wget-ssl=y
CONFIG_PACKAGE_ca-certificates=y
CONFIG_PACKAGE_kmod-tcp-bbr=y

# ===== 精简: 去掉用不到的东西 =====
# CONFIG_PACKAGE_luci-app-opkg is not set
# CONFIG_PACKAGE_kmod-usb-printer is not set
